collections.defaultdict: consider converting to a default dict when using in templates

{{ data|default:"3 < 2" }}  {# Bad! Don't do this. #}

/home/kiswelrg/.local/share/jupyter/kernels/jupyterfordjango/kernel.json

